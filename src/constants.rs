//! アプリケーション全体で使用する定数

// =============================================================================
// パス・ファイル関連
// =============================================================================

/// アプリケーションディレクトリ名
pub const APP_DIR_NAME: &str = ".gymeat";

/// 履歴ディレクトリ名
pub const HISTORY_DIR_NAME: &str = "history";

/// プランディレクトリ名
pub const PLANS_DIR_NAME: &str = "plans";

/// インデックスファイル名
pub const INDEX_FILE_NAME: &str = "index.json";

/// 一時ファイルのプレフィックス
pub const TEMP_FILE_PREFIX: &str = "gymeat_temp";

// =============================================================================
// バリデーション範囲
// =============================================================================

/// 体重の最大値 (kg)
pub const MAX_WEIGHT_KG: f64 = 300.0;

/// 身長の最小値 (cm)
pub const MIN_HEIGHT_CM: f64 = 100.0;

/// 身長の最大値 (cm)
pub const MAX_HEIGHT_CM: f64 = 250.0;

/// カロリーの最小値 (kcal)
pub const MIN_CALORIES: f64 = 500.0;

/// カロリーの最大値 (kcal)
pub const MAX_CALORIES: f64 = 10000.0;

// =============================================================================
// カロリー調整
// =============================================================================

/// 増量時のカロリー増加量 (kcal)
pub const BULK_CALORIE_SURPLUS: f64 = 300.0;

/// 減量時のカロリー削減量 (kcal)
pub const CUT_CALORIE_DEFICIT: f64 = 500.0;

// =============================================================================
// デフォルトカロリー
// =============================================================================

/// 増量時のデフォルトカロリー (kcal)
pub const DEFAULT_CALORIES_BULK: f64 = 2800.0;

/// 減量時のデフォルトカロリー (kcal)
pub const DEFAULT_CALORIES_CUT: f64 = 2000.0;

/// 維持時のデフォルトカロリー (kcal)
pub const DEFAULT_CALORIES_MAINTAIN: f64 = 2400.0;

// =============================================================================
// 達成度判定
// =============================================================================

/// 達成度の下限 (%)
pub const ACHIEVEMENT_LOWER_BOUND: f64 = 90.0;

/// 達成度の上限 (%)
pub const ACHIEVEMENT_UPPER_BOUND: f64 = 110.0;

// =============================================================================
// ID関連
// =============================================================================

/// 短縮ID表示時の文字数
pub const SHORT_ID_LENGTH: usize = 8;
